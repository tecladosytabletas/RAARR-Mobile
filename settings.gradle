pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        // Para MPCharts
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "AppAtemporal"
include ':app'
